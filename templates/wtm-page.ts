import { BasePageComponent } from "@everys/aurelia-components-abstractions";
import { autoinject } from "aurelia-framework";

@autoinject
export class $(CebabToCamelCase($Name)) extends BasePageComponent {
  constructor(
  ) {
    super("$(CebabToCamelCase($Name))");
  }
}
